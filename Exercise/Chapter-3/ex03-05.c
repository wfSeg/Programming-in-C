#include <stdio.h>

int main (void)
{
	int sum;		// capitalization
	/*compute result */	//need to close with */
	sum = 25 + 37 -19;		// need the ;
	/* display reults */ 	// use correct closing slashes, probably need printf what question was in first place
	printf ("The sum of 25 + 37 - 19 is %i\n", sum);
	return 0;
}
